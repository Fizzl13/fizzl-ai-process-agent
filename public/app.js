const $=id=>document.getElementById(id); const result=$('result');
$('run').onclick=async()=>{const message=$('message').value.trim();if(!message)return;result.innerHTML='<div class="muted">AI is analyzing…</div>';try{const r=await fetch('/api/process-case',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({message})});const d=await r.json();if(!r.ok)throw new Error(d.error||'Request failed');window.lastCase=d;render(d)}catch(e){result.innerHTML=`<div class="blocked">${e.message}</div>`}};
function render(d){result.innerHTML=`<div class="row"><b>${d.caseId}</b><br><span class="muted">${d.status}</span></div><div class="row"><div class="muted">RISK</div><div class="risk">${d.decision.risk}</div><div class="muted">${d.decision.reason}</div></div><div class="row"><b>AI ANALYSIS</b><pre>${JSON.stringify(d.analysis,null,2)}</pre></div><div class="row"><b>ACTION PLAN</b><pre>${JSON.stringify(d.actions,null,2)}</pre></div><div class="row"><b>EXECUTION</b><div class="blocked">${d.execution.allowed?'DEMO EXECUTION ALLOWED':'BLOCKED — HUMAN CONTROL REQUIRED'}</div>${d.decision.humanRequired?'<button id="approve">APPROVE DEMO ACTION</button>':''}</div>`;const a=$('approve');if(a)a.onclick=approve}
async function approve(){const r=await fetch('/api/approve-action',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({caseId:window.lastCase.caseId})});const d=await r.json();window.lastCase=d;render(d)}
